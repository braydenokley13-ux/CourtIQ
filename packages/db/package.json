{
  "name": "@courtiq/db",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prisma:validate": "prisma validate --schema=./prisma/schema.prisma",
    "build": "prisma generate",
    "typecheck": "echo 'No TypeScript sources in db package yet'"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@courtiq/config": "workspace:*",
    "prisma": "^5.22.0",
    "typescript": "^5.6.3"
  }
}
